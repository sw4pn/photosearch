import { Button } from "@/components/ui/Button";

const DownloadButton = ({ image, size }) => {
  return <Button className="bg-green-500 w-full">Download for free!</Button>;
};

export default DownloadButton;
